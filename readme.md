# Instructions

## show.yml
Please create a customized credential type using the template name "SFTP_Cred_type.yml" 

The command that ansbile run are in Extra Variables

```yaml
cli_cmd:
  - show int description | inc down
  - show inventory
```
